import Task from "./Task";
const Tasks = (props) => {
  return (
    <ul>
      {/* {props.tasks.map((task, index) => {
        return <Tasks taskName={task} key={index} />;
      })} */}
    </ul>
  );
};
export default Tasks;
